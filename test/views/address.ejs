<!DOCTYPE html>
<html>
  <head>
    <title>도서구매사이트</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@48,400,0,0" />
  </head>
  <body>
    <nav class="navbar navbar-expand-lg bg-light">
      <div class="container-fluid" style="width: 960px;">
        <a class="navbar-brand" href="/">도서구매사이트</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href="/">Home</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="book">Book</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="modal">Modal TEST</a>
            </li>
            </li>
            <li class="nav-item">
              <a class="nav-link disabled">Disabled</a>
            </li>
          </ul>
          <form class="d-flex">
            <% if(signinStatus) {%>
            <a class="btn btn-outline-primary" href="/mypage">마이페이지</a>&nbsp;
            <a class="btn btn-primary" href="/signin">로그아웃</a>
            <% } else {%>
            <a class="btn btn-outline-primary" href="/signin">로그인</a>&nbsp;
            <a class="btn btn-primary" href="/signup">회원가입</a>
            <% }%>
          </form>
        </div>
      </div>
    </nav>
    <div id="main">
      <h3>새로운 배송지 추가하기</h3>
      <form class="d-flex" action="address" method="post">
        <div class="input-group">
          <span class="input-group-text"><span class="material-symbols-outlined" style="vertical-align: middle;">home</span></span>
          <input class="form-control" name="zipcode" type="text" placeholder="우편번호" >
          <input class="form-control" name="main" type="text" placeholder="기본주소">
          <input class="form-control" name="detail" type="text" placeholder="상세주소">
          <button type="submit" class="btn btn-primary" style="margin-left: auto; margin-right: 0;">+ 추가</button>
        </div>
      </form><br>
      <h3>배송지 목록</h3>
      <% for(var i=0; i < num; i++) { %>
        <div class="card">
          <div class="card-header">
            <%= i+1 %>
          </div>
          <div class="card-body">
            <form action="address/delete/<%= address[i].id %>" method="post">
              <button type="submit" class="btn btn-danger" style="float: right; margin-left: 8px;">삭제</button>
            </form>
            <form action="address/modify/<%= address[i].id %>" method="post">
              <div class="input-group" style="width: auto;">
                <span class="input-group-text"><span class="material-symbols-outlined" style="vertical-align: middle;">home</span></span>
                <input class="form-control" name="zipcode" type="text" placeholder="<%= address[i].zipcode %>" >
                <input class="form-control" name="main" type="text" placeholder="<%= address[i].main %>">
                <input class="form-control" name="detail" type="text" placeholder="<%= address[i].detail %>">
                <button type="submit" class="btn btn-primary" style="margin-left: auto; margin-right: 0;">수정</button>
              </div>
            </form>
          </div>
        </div>
        <br>
      <% } %>
      
    </div>
  </body>
</html>
