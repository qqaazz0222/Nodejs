<!DOCTYPE html>
<html>
  <head>
    <title>도서구매사이트</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@48,400,0,0" />
  </head>
  <body>
    <nav class="navbar navbar-expand-lg bg-light">
      <div class="container-fluid" style="width: 960px;">
        <a class="navbar-brand" href="/">도서구매사이트</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href="/">Home</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="book">Book</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="modal">Modal TEST</a>
            </li>
            </li>
            <li class="nav-item">
              <a class="nav-link disabled">Disabled</a>
            </li>
          </ul>
          <form class="d-flex">
            <% if(signinStatus) {%>
            <a class="btn btn-outline-primary" href="/mypage">마이페이지</a>&nbsp;
            <a class="btn btn-primary" href="/signin">로그아웃</a>
            <% } else {%>
            <a class="btn btn-outline-primary" href="/signin">로그인</a>&nbsp;
            <a class="btn btn-primary" href="/signup">회원가입</a>
            <% }%>
          </form>
        </div>
      </div>
    </nav>
    <div id="main">
      <div class="alert alert-primary" role="alert">
        <span class="material-symbols-outlined" style="vertical-align: middle;">notifications_active</span>
        도서구매사이트를 열심히 만들어 보자!
      </div>
      <% for (var i = 0; i < num; i++) { if((i+1)%5==0) { %>
        <a href="/book/<%= books[i].id %>" style="text-decoration: none; text-decoration-line: none">
          <div class="card" style="width: 180px; float: left; margin-bottom: 16px;">
            <img src="images/book<%= books[i].id%>.png" class="card-img-top" alt="...">
            <div class="card-body">
              <h5 class="card-title" style="white-space:nowrap; overflow:hidden; text-overflow:ellipsis;"><%= books[i].title %></h5>
              <p class="card-text">
                <span class="material-symbols-outlined" style="vertical-align: middle; font-size: medium;">sell</span>
                <%= books[i].price.toString().replace(/\B(?=(\d{3})+(?!\d))/g, ',') %>원</p>
            </div>
          </div>
        </a>
      <% } else {%>
        <a href="/book/<%= books[i].id %>" style="text-decoration: none; text-decoration-line: none">
          <div class="card" style="width: 180px; float: left; margin-right: 15px; margin-bottom: 16px;">
            <img src="images/book<%= books[i].id%>.png" class="card-img-top" alt="...">
            <div class="card-body">
              <h5 class="card-title" style="white-space:nowrap; overflow:hidden; text-overflow:ellipsis;"><%= books[i].title %></h5>
              <p class="card-text">
                <span class="material-symbols-outlined" style="vertical-align: middle; font-size: medium;">sell</span>
                <%= books[i].price.toString().replace(/\B(?=(\d{3})+(?!\d))/g, ',') %>원</p>
            </div>
          </div>
        </a>
      <% }} %>
    </div>
  </body>
</html>
